{
  "name": "pip-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "A tiny mouse that helps your users.",
  "license": "MIT",
  "author": "Matt White",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MPWhite/onboarding-driver.git"
  },
  "packageManager": "pnpm@10.6.4",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@changesets/cli": "^2.30.0",
    "turbo": "^2.3.0",
    "typescript": "^5.6.0"
  }
}
